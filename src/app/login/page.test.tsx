import { describe, it } from 'vitest';

describe('LoginPage', () => {
  it.skip('requires E2E testing due to useRouter dependency', () => {
    // LoginPage uses useRouter() which requires Next.js app router context.
    // Unit tests cannot provide this. Real validation happens in E2E tests.
  });
});
